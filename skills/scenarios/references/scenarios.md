<!-- GENERATED by tools/build-refs.py — edit notes/scenarios.md -->

# Practice scenarios API reference

This reference is generated from the committed OpenAPI 3.0.1 snapshots. Schema tables use only `paths` and `components.schemas`; curated behavior comes from the marked notes blocks below.

Use the gateway host unless an operation is explicitly marked as using the practice host. Read the API key from `ITERO_API_KEY` and never print it.

## Verified guidance

<!-- gotchas -->
## Shared API gotchas

<!-- fact:auth-header -->
### Authenticate without exposing the key

Use `https://iterogatewayapi.azurewebsites.net` for Itero public API operations unless a resource note names a verified exception. Send the key in this header:

```http
X-API-Key: <key>
```

Read the default key from `ITERO_API_KEY`. For a named tenant, read `ITERO_API_KEY_<NAME>`, with the tenant name upper-cased. Never paste the key into a command that will be saved, print it, log it, or include it in an error message.

<!-- fact:context-safety-projection -->
### Keep large collections out of the agent context

Do not read a full collection response into the conversation when a few fields will answer the question. Save large responses to a file or project them to the smallest useful shape first.

This matters most for `GET /api/public/v1/practice-scenario`: the verified tenant returned 596 scenarios, about 535 KB or 152,000 tokens. Projecting each item to its ID, display name, and `personaId` reduced the response to about 48 KB. It also matters for `GET /api/public/v1/call/get-call`: save the full call to a file and extract only the needed transcript fields instead of loading the entire response.

Apply the same rule to every list operation: filter on the server when possible, select only fields the user needs, and redirect raw JSON to a file when detail must be preserved.
<!-- /gotchas -->

<!-- gotchas -->
## Practice scenario gotchas

### Write behavior from the simulated person's point of view

Use `keyBehaviorsOpinions` to describe what the simulated person knows, believes, and tends to do. Keep stable archetype traits on the persona and scenario-specific facts on the scenario. A useful structure is a short `Context` section followed by `Key Behaviors and Opinions`, written in second person.

For a live-call simulation (`practiceScenarioType: 2`), provide the transcript and do not invent a separate behavior script that conflicts with it. For other scenario types, provide enough behavioral direction to make the practice specific and winnable.

### Treat updates as complete-object writes

Fetch the current scenario before an update and carry forward fields that should remain unchanged. Review linked persona, call type, communication style, scorecard, dialogue-start setting, and persona override fields before sending the complete payload.

<!-- fact:scenario-roundtrip-overrides -->
### Do not round-trip synthesized persona overrides

`GET /practice-scenario` can return synthesized values in `personaBotName`, `personaCompany`, and `personaTitle` — auto-filled placeholders (for example an invented company name), not stored scenario data. A naive fetch-then-PUT persists those generated values as real overrides, silently clobbering what was set at create time.

Before any PUT: explicitly set `personaBotName` to the intended value, and set `personaCompany` and `personaTitle` to `null` unless the scenario genuinely needs a B2B company/title override. Show these three fields in the confirmation preview so the user sees exactly what will be stored. (Field-verified 2026-04; re-encoded 2026-08-12.)

List responses can be very large. Follow the shared context-safety rule: project IDs and names for selection, and save the full JSON to a file only when a detailed edit requires it.
<!-- /gotchas -->

## Endpoint map

| Method | Path | Host | Success |
|---|---|---|---|
| `GET` | `/api/public/v1/practice-scenario` | Gateway | `200` |
| `POST` | `/api/public/v1/practice-scenario` | Gateway | `200` |
| `PUT` | `/api/public/v1/practice-scenario` | Gateway | `200` |
| `GET` | `/api/public/v1/practice-scenario/call-types` | Gateway | `200` |
| `GET` | `/api/public/v1/practice-scenario/communication-styles` | Gateway | `200` |
| `DELETE` | `/api/public/v1/practice-scenario/{id}` | Gateway | `200` |

## Operations

### `GET /api/public/v1/practice-scenario`

#### Request schema

This operation has no parameters or request body.

#### Example

Replace the example identifier or query value, then run this read request. The raw response is saved to `practice-scenarios.json`; project only the fields you need before bringing any result into the conversation.

```bash
curl --fail-with-body --silent --show-error \
  --request GET \
  --header "X-API-Key: $ITERO_API_KEY" \
  "https://iterogatewayapi.azurewebsites.net/api/public/v1/practice-scenario" \
  --output "practice-scenarios.json"
```

#### Success response schema

Status `200`:

| Field | Type | Required | Nullable | Allowed values |
|---|---|:---:|:---:|---|
| `items[].activityHistories` | `array<PracticeScenarioActivityHistoryDto>` | No | Yes | — |
| `items[].activityHistories[].activity` | `string` | No | Yes | — |
| `items[].activityHistories[].date` | `string (date-time)` | No | No | — |
| `items[].activityHistories[].id` | `integer (int32)` | No | No | — |
| `items[].dialogueStartSetting` | `integer enum` | No | No | `0`, `1`, `2` |
| `items[].endCallFunctionExpression` | `string` | No | Yes | — |
| `items[].id` | `integer (int32)` | No | No | — |
| `items[].internalSystems` | `array<PracticeScenarioInternalSystemDto>` | No | Yes | — |
| `items[].internalSystems[].attributes` | `array<PracticeScenarionInternalSystemAttributeDto>` | No | Yes | — |
| `items[].internalSystems[].attributes[].attribute` | `string` | No | Yes | — |
| `items[].internalSystems[].attributes[].id` | `integer (int32)` | No | No | — |
| `items[].internalSystems[].attributes[].value` | `string` | No | Yes | — |
| `items[].internalSystems[].systemName` | `string` | No | Yes | — |
| `items[].keyBehaviorsOpinions` | `string` | No | Yes | — |
| `items[].omitFromScoring` | `boolean` | No | No | — |
| `items[].personaBotName` | `string` | No | Yes | — |
| `items[].personaCompany` | `string` | No | Yes | — |
| `items[].personaId` | `integer (int32)` | No | No | — |
| `items[].personaTitle` | `string` | No | Yes | — |
| `items[].practiceScenarioCallTypeId` | `integer (int32)` | No | Yes | — |
| `items[].practiceScenarioCommunicationStyleId` | `integer (int32)` | No | Yes | — |
| `items[].practiceScenarioDescription` | `string` | No | Yes | — |
| `items[].practiceScenarioName` | `string` | No | Yes | — |
| `items[].practiceScenarioType` | `integer enum` | No | No | `0` (CommonScenario), `1` (ObjectionHandling), `2` (LiveCallSimulation), `3` (FocusScenario) |
| `items[].scorecardTemplateId` | `integer (int32)` | No | Yes | — |
| `items[].starterLine` | `string` | No | Yes | — |
| `items[].transcript` | `string` | No | Yes | — |

#### Error responses

| Status | Meaning |
|---:|---|
| `401` | The API key is missing or invalid. |
| `403` | The API key does not have permission for this operation. |
| `500` | The service returned an internal error. |

---

### `POST /api/public/v1/practice-scenario`

#### Request schema

| Field | Type | Required | Nullable | Allowed values |
|---|---|:---:|:---:|---|
| `body: activityHistories` | `array<PracticeScenarioActivityHistoryDto>` | No | Yes | — |
| `body: activityHistories[].activity` | `string` | No | Yes | — |
| `body: activityHistories[].date` | `string (date-time)` | No | No | — |
| `body: activityHistories[].id` | `integer (int32)` | No | No | — |
| `body: dialogueStartSetting` | `integer enum` | No | No | `0`, `1`, `2` |
| `body: endCallFunctionExpression` | `string` | No | Yes | — |
| `body: internalSystems` | `array<PracticeScenarioInternalSystemDto>` | No | Yes | — |
| `body: internalSystems[].attributes` | `array<PracticeScenarionInternalSystemAttributeDto>` | No | Yes | — |
| `body: internalSystems[].attributes[].attribute` | `string` | No | Yes | — |
| `body: internalSystems[].attributes[].id` | `integer (int32)` | No | No | — |
| `body: internalSystems[].attributes[].value` | `string` | No | Yes | — |
| `body: internalSystems[].systemName` | `string` | No | Yes | — |
| `body: keyBehaviorsOpinions` | `string` | No | Yes | — |
| `body: omitFromScoring` | `boolean` | No | No | — |
| `body: personaBotName` | `string` | No | Yes | — |
| `body: personaCompany` | `string` | No | Yes | — |
| `body: personaId` | `integer (int32)` | No | No | — |
| `body: personaTitle` | `string` | No | Yes | — |
| `body: practiceScenarioCallTypeId` | `integer (int32)` | No | Yes | — |
| `body: practiceScenarioCommunicationStyleId` | `integer (int32)` | No | Yes | — |
| `body: practiceScenarioDescription` | `string` | No | Yes | — |
| `body: practiceScenarioName` | `string` | No | Yes | — |
| `body: practiceScenarioType` | `integer enum` | No | No | `0` (CommonScenario), `1` (ObjectionHandling), `2` (LiveCallSimulation), `3` (FocusScenario) |
| `body: scorecardTemplateId` | `integer (int32)` | No | Yes | — |
| `body: starterLine` | `string` | No | Yes | — |
| `body: transcript` | `string` | No | Yes | — |

Body media type: `application/json`.

#### Example

Show this exact payload to the user and wait for explicit confirmation before running the request.

```bash
curl --fail-with-body --silent --show-error \
  --request POST \
  --header "X-API-Key: $ITERO_API_KEY" \
  --header "Content-Type: application/json" \
  --data '{
  "activityHistories": [
    {
      "id": 123,
      "activity": "string",
      "date": "2026-08-12T15:00:00Z"
    }
  ],
  "dialogueStartSetting": 0,
  "endCallFunctionExpression": "string",
  "internalSystems": [
    {
      "attributes": [
        {
          "id": 123,
          "attribute": null,
          "value": null
        }
      ],
      "systemName": "Example"
    }
  ],
  "keyBehaviorsOpinions": "string"
}' \
  "https://iterogatewayapi.azurewebsites.net/api/public/v1/practice-scenario"
```

#### Success response schema

Status `200`:

| Field | Type | Required | Nullable | Allowed values |
|---|---|:---:|:---:|---|
| `activityHistories` | `array<PracticeScenarioActivityHistoryDto>` | No | Yes | — |
| `activityHistories[].activity` | `string` | No | Yes | — |
| `activityHistories[].date` | `string (date-time)` | No | No | — |
| `activityHistories[].id` | `integer (int32)` | No | No | — |
| `dialogueStartSetting` | `integer enum` | No | No | `0`, `1`, `2` |
| `endCallFunctionExpression` | `string` | No | Yes | — |
| `id` | `integer (int32)` | No | No | — |
| `internalSystems` | `array<PracticeScenarioInternalSystemDto>` | No | Yes | — |
| `internalSystems[].attributes` | `array<PracticeScenarionInternalSystemAttributeDto>` | No | Yes | — |
| `internalSystems[].attributes[].attribute` | `string` | No | Yes | — |
| `internalSystems[].attributes[].id` | `integer (int32)` | No | No | — |
| `internalSystems[].attributes[].value` | `string` | No | Yes | — |
| `internalSystems[].systemName` | `string` | No | Yes | — |
| `keyBehaviorsOpinions` | `string` | No | Yes | — |
| `omitFromScoring` | `boolean` | No | No | — |
| `personaBotName` | `string` | No | Yes | — |
| `personaCompany` | `string` | No | Yes | — |
| `personaId` | `integer (int32)` | No | No | — |
| `personaTitle` | `string` | No | Yes | — |
| `practiceScenarioCallTypeId` | `integer (int32)` | No | Yes | — |
| `practiceScenarioCommunicationStyleId` | `integer (int32)` | No | Yes | — |
| `practiceScenarioDescription` | `string` | No | Yes | — |
| `practiceScenarioName` | `string` | No | Yes | — |
| `practiceScenarioType` | `integer enum` | No | No | `0` (CommonScenario), `1` (ObjectionHandling), `2` (LiveCallSimulation), `3` (FocusScenario) |
| `scorecardTemplateId` | `integer (int32)` | No | Yes | — |
| `starterLine` | `string` | No | Yes | — |
| `transcript` | `string` | No | Yes | — |

#### Error responses

| Status | Meaning |
|---:|---|
| `401` | The API key is missing or invalid. |
| `403` | The API key does not have permission for this operation. |
| `500` | The service returned an internal error. |

---

### `PUT /api/public/v1/practice-scenario`

#### Request schema

| Field | Type | Required | Nullable | Allowed values |
|---|---|:---:|:---:|---|
| `body: activityHistories` | `array<PracticeScenarioActivityHistoryDto>` | No | Yes | — |
| `body: activityHistories[].activity` | `string` | No | Yes | — |
| `body: activityHistories[].date` | `string (date-time)` | No | No | — |
| `body: activityHistories[].id` | `integer (int32)` | No | No | — |
| `body: dialogueStartSetting` | `integer enum` | No | No | `0`, `1`, `2` |
| `body: endCallFunctionExpression` | `string` | No | Yes | — |
| `body: id` | `integer (int32)` | No | No | — |
| `body: internalSystems` | `array<PracticeScenarioInternalSystemDto>` | No | Yes | — |
| `body: internalSystems[].attributes` | `array<PracticeScenarionInternalSystemAttributeDto>` | No | Yes | — |
| `body: internalSystems[].attributes[].attribute` | `string` | No | Yes | — |
| `body: internalSystems[].attributes[].id` | `integer (int32)` | No | No | — |
| `body: internalSystems[].attributes[].value` | `string` | No | Yes | — |
| `body: internalSystems[].systemName` | `string` | No | Yes | — |
| `body: keyBehaviorsOpinions` | `string` | No | Yes | — |
| `body: omitFromScoring` | `boolean` | No | No | — |
| `body: personaBotName` | `string` | No | Yes | — |
| `body: personaCompany` | `string` | No | Yes | — |
| `body: personaId` | `integer (int32)` | No | No | — |
| `body: personaTitle` | `string` | No | Yes | — |
| `body: practiceScenarioCallTypeId` | `integer (int32)` | No | Yes | — |
| `body: practiceScenarioCommunicationStyleId` | `integer (int32)` | No | Yes | — |
| `body: practiceScenarioDescription` | `string` | No | Yes | — |
| `body: practiceScenarioName` | `string` | No | Yes | — |
| `body: practiceScenarioType` | `integer enum` | No | No | `0` (CommonScenario), `1` (ObjectionHandling), `2` (LiveCallSimulation), `3` (FocusScenario) |
| `body: scorecardTemplateId` | `integer (int32)` | No | Yes | — |
| `body: starterLine` | `string` | No | Yes | — |
| `body: transcript` | `string` | No | Yes | — |

Body media type: `application/json`.

#### Example

Show this exact payload to the user and wait for explicit confirmation before running the request.

```bash
curl --fail-with-body --silent --show-error \
  --request PUT \
  --header "X-API-Key: $ITERO_API_KEY" \
  --header "Content-Type: application/json" \
  --data '{
  "id": 123,
  "activityHistories": [
    {
      "id": 123,
      "activity": "string",
      "date": "2026-08-12T15:00:00Z"
    }
  ],
  "dialogueStartSetting": 0,
  "endCallFunctionExpression": "string",
  "internalSystems": [
    {
      "attributes": [
        {
          "id": 123,
          "attribute": null,
          "value": null
        }
      ],
      "systemName": "Example"
    }
  ]
}' \
  "https://iterogatewayapi.azurewebsites.net/api/public/v1/practice-scenario"
```

#### Success response schema

Status `200`:

| Field | Type | Required | Nullable | Allowed values |
|---|---|:---:|:---:|---|
| `activityHistories` | `array<PracticeScenarioActivityHistoryDto>` | No | Yes | — |
| `activityHistories[].activity` | `string` | No | Yes | — |
| `activityHistories[].date` | `string (date-time)` | No | No | — |
| `activityHistories[].id` | `integer (int32)` | No | No | — |
| `dialogueStartSetting` | `integer enum` | No | No | `0`, `1`, `2` |
| `endCallFunctionExpression` | `string` | No | Yes | — |
| `id` | `integer (int32)` | No | No | — |
| `internalSystems` | `array<PracticeScenarioInternalSystemDto>` | No | Yes | — |
| `internalSystems[].attributes` | `array<PracticeScenarionInternalSystemAttributeDto>` | No | Yes | — |
| `internalSystems[].attributes[].attribute` | `string` | No | Yes | — |
| `internalSystems[].attributes[].id` | `integer (int32)` | No | No | — |
| `internalSystems[].attributes[].value` | `string` | No | Yes | — |
| `internalSystems[].systemName` | `string` | No | Yes | — |
| `keyBehaviorsOpinions` | `string` | No | Yes | — |
| `omitFromScoring` | `boolean` | No | No | — |
| `personaBotName` | `string` | No | Yes | — |
| `personaCompany` | `string` | No | Yes | — |
| `personaId` | `integer (int32)` | No | No | — |
| `personaTitle` | `string` | No | Yes | — |
| `practiceScenarioCallTypeId` | `integer (int32)` | No | Yes | — |
| `practiceScenarioCommunicationStyleId` | `integer (int32)` | No | Yes | — |
| `practiceScenarioDescription` | `string` | No | Yes | — |
| `practiceScenarioName` | `string` | No | Yes | — |
| `practiceScenarioType` | `integer enum` | No | No | `0` (CommonScenario), `1` (ObjectionHandling), `2` (LiveCallSimulation), `3` (FocusScenario) |
| `scorecardTemplateId` | `integer (int32)` | No | Yes | — |
| `starterLine` | `string` | No | Yes | — |
| `transcript` | `string` | No | Yes | — |

#### Error responses

| Status | Meaning |
|---:|---|
| `400` | The request failed validation. Check field names, types, and values. |
| `401` | The API key is missing or invalid. |
| `403` | The API key does not have permission for this operation. |
| `404` | The requested resource was not found. |
| `500` | The service returned an internal error. |

---

### `GET /api/public/v1/practice-scenario/call-types`

#### Request schema

This operation has no parameters or request body.

#### Example

Replace the example identifier or query value, then run this read request. The raw response is saved to `call-types.json`; project only the fields you need before bringing any result into the conversation.

```bash
curl --fail-with-body --silent --show-error \
  --request GET \
  --header "X-API-Key: $ITERO_API_KEY" \
  "https://iterogatewayapi.azurewebsites.net/api/public/v1/practice-scenario/call-types" \
  --output "call-types.json"
```

#### Success response schema

Status `200`:

| Field | Type | Required | Nullable | Allowed values |
|---|---|:---:|:---:|---|
| `items[].description` | `string` | No | Yes | — |
| `items[].id` | `integer (int32)` | No | No | — |
| `items[].name` | `string` | No | Yes | — |

#### Error responses

| Status | Meaning |
|---:|---|
| `401` | The API key is missing or invalid. |
| `403` | The API key does not have permission for this operation. |
| `500` | The service returned an internal error. |

---

### `GET /api/public/v1/practice-scenario/communication-styles`

#### Request schema

This operation has no parameters or request body.

#### Example

Replace the example identifier or query value, then run this read request. The raw response is saved to `communication-styles.json`; project only the fields you need before bringing any result into the conversation.

```bash
curl --fail-with-body --silent --show-error \
  --request GET \
  --header "X-API-Key: $ITERO_API_KEY" \
  "https://iterogatewayapi.azurewebsites.net/api/public/v1/practice-scenario/communication-styles" \
  --output "communication-styles.json"
```

#### Success response schema

Status `200`:

| Field | Type | Required | Nullable | Allowed values |
|---|---|:---:|:---:|---|
| `items[].description` | `string` | No | Yes | — |
| `items[].id` | `integer (int32)` | No | No | — |
| `items[].name` | `string` | No | Yes | — |

#### Error responses

| Status | Meaning |
|---:|---|
| `401` | The API key is missing or invalid. |
| `403` | The API key does not have permission for this operation. |
| `500` | The service returned an internal error. |

---

### `DELETE /api/public/v1/practice-scenario/{id}`

#### Request schema

| Field | Type | Required | Nullable | Allowed values |
|---|---|:---:|:---:|---|
| `path: id` | `integer (int32)` | Yes | No | — |

#### Example

Show this exact payload to the user and wait for explicit confirmation before running the request.

```bash
curl --fail-with-body --silent --show-error \
  --request DELETE \
  --header "X-API-Key: $ITERO_API_KEY" \
  "https://iterogatewayapi.azurewebsites.net/api/public/v1/practice-scenario/123"
```

#### Success response schema

Status `200`:

The response has no body.

#### Error responses

| Status | Meaning |
|---:|---|
| `401` | The API key is missing or invalid. |
| `403` | The API key does not have permission for this operation. |
| `500` | The service returned an internal error. |
